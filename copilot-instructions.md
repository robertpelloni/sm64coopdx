# GitHub Copilot Instructions
*Reference `AGENTS.md` for global rules.*

When generating code completions for this project:
1. Prefer using `_G.UIToolkit` for any new UI code.
2. Always add verbose comments explaining the logic.
3. Use `gPlayerSyncTable` for networked variables, ensuring keys are flat strings.
4. Ensure numerical inputs are validated as `> 0`.
