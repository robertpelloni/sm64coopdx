# Waypoints System

Manages seamless world level connections and Fast Travel discovery points.
Relies on `mod_storage` for save persistence and `system_ui` for menu rendering.
Integrated into the Main Menu.
