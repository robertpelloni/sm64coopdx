# GPT Specific Instructions
*Reference `AGENTS.md` for global rules.*
*Current Project Version: Reference VERSION.md for the single source of truth.*

1. **Code Synthesis:** Focus on writing extremely robust, elegant, and heavily commented code. Ensure every edge case (player disconnects, integer overflows, UI clipping) is handled.
2. **Tool Usage:** Use your bash session effectively. `grep` aggressively to find dependencies before refactoring APIs.
3. **Momentum:** Adhere strictly to the "never stop" directive. Autonomously select the next logical task from the roadmap and execute.
